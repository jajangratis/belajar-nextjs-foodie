'use client'
export default function Error({error}) {
    return (
        <main className="error">
            <h1>Error While occuring data!</h1>
            <p>Failed to created meal</p>
        </main>
    )
}